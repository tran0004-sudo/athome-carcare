name: Archived homepage patch

"on":
  workflow_dispatch:

permissions:
  contents: read

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Archived workflow
        run: echo "Legacy one-time homepage patch is archived. No automatic action is required."
